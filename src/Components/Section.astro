---
const { title, description, imageUrl } = Astro.props;
---
<section id="about" class="mb-16 flex flex-col md:flex-row items-center">
  <div class="w-full md:w-1/3 mb-4 md:mb-0">
    <img src={imageUrl} alt={`Imagen de ${title}`} class="rounded-full shadow-md w-full h-auto hover:shadow-2xl transition duration-300 transform hover:scale-105" />
  </div>
  <div class="w-full md:w-2/3">
    <!-- Título con estilo atractivo -->
    <h1 class="text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-indigo-500 mb-4 drop-shadow-lg">
      Sobre Mi {title}
    </h1>
    <p class="text-lg text-gray-800 mb-4 font-light tracking-wide leading-relaxed">
      {description}
    </p>
  </div>
</section>
